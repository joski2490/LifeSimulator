name: Node.js Start

on:
 workflow_dispatch:
 
jobs:
 start:
 
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v3
  - name: Node.js
    uses: actions/setup-node@v3
    with:
     node-version: '16.x'
     cache: 'npm'
  - run: npm install
  - run: npm ci
  - run: npm start
